export type PageSummary = {
    title: string;
    description: string;
    embeddedImages: string[];
    logos: string[];
    photos: string[];
  };
  
  export type MediaType = "image" | "logo" | "photo";